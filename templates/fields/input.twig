<input type="{{ type }}" name="{{ name }}" value="{{ value }}" id="{{ id }}"{% for key, attribute in attributes %}{% if attribute != 'false' %} {{ key }}{% if attribute != 'true' %}="{{ attribute }}"{% endif %}{% endif %}{% endfor %}>